services:
  web:
    image: nginx:1.27-alpine
    container_name: guizhou-3d-hall
    restart: unless-stopped
    ports:
      - "20515:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
